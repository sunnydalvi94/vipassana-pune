<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');
$mode = "local";
$send_sms_flag = false; // flag for sending single sms true-send, false- not send

$email_mode = false; /* Email mode, Set True for live and false for development */
$send_email_flag= true; /* Email send, Prevent == True to prevent email sending */